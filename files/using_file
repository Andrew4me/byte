poem = '''\
    Программировать весело
    Если работа скучна,
    Чтобы придать ей веселый тон - 
        используй питон!
'''

f = open('poem.txt', 'w') # открываем для записи (w - writing)
f.write(poem) # записываем текст в файл
f.close() # закрываем файл

f = open('poem.txt') # если режим не указан, по умолчанию подразумевается режи - чтение('r'eading)

while True:
    line = f.readline()
    if len(line) == 0: # Нулевая длина обозначает конец файла (EOF)
        break
    print(line, end='')

f.close() # закрываем файл


